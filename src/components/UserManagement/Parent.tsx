import React from 'react';

const Parent: React.FC = () => {
    return (
        <div>
            <h2>Parent Dashboard</h2>
            <p>Monitor children's progress and attendance.</p>
        </div>
    );
};

export default Parent;