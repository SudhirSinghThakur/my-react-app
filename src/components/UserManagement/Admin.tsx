import React from 'react';

const Admin: React.FC = () => {
    return (
        <div>
            <h2>Admin Dashboard</h2>
            <p>Manage school settings, users, and reports.</p>
        </div>
    );
};

export default Admin;