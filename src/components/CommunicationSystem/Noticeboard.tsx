import React from 'react';

const Noticeboard: React.FC = () => {
    return (
        <div>
            <h2>Noticeboard</h2>
            <p>View announcements and updates.</p>
        </div>
    );
};

export default Noticeboard;